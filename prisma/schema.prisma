// Prisma schema for the personal dashboard.
// SQLite has no native enum type, so area/status/priority/frequency are
// plain strings; the allowed values are enforced in TypeScript (see
// src/types/index.ts) rather than in the schema.

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model Task {
  id          String    @id @default(cuid())
  title       String
  description String?
  area        String // "BUSINESS" | "PERSONAL"
  status      String    @default("TODO") // "TODO" | "IN_PROGRESS" | "DONE"
  priority    String    @default("MEDIUM") // "LOW" | "MEDIUM" | "HIGH" | "URGENT"
  dueDate     DateTime?
  order       Int       @default(0)
  aiGenerated Boolean   @default(false)

  projectId String?
  project   Project? @relation(fields: [projectId], references: [id], onDelete: SetNull)

  parentTaskId String?
  parentTask   Task?  @relation("TaskSubtasks", fields: [parentTaskId], references: [id], onDelete: Cascade)
  subtasks     Task[] @relation("TaskSubtasks")

  notes Note[]

  completedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@index([area, status])
  @@index([projectId])
  @@index([parentTaskId])
  @@index([dueDate])
}

model Project {
  id          String    @id @default(cuid())
  name        String
  description String?
  area        String // "BUSINESS" | "PERSONAL"
  status      String    @default("ACTIVE") // "PLANNED" | "ACTIVE" | "ON_HOLD" | "COMPLETED"
  color       String?
  dueDate     DateTime?
  order       Int       @default(0)

  tasks Task[]
  notes Note[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([area, status])
}

model Note {
  id      String  @id @default(cuid())
  title   String
  content String // markdown
  area    String // "BUSINESS" | "PERSONAL"
  tags    String? // comma-separated
  pinned  Boolean @default(false)

  projectId String?
  project   Project? @relation(fields: [projectId], references: [id], onDelete: SetNull)

  taskId String?
  task   Task?   @relation(fields: [taskId], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([area])
}

model Habit {
  id                String  @id @default(cuid())
  name              String
  area              String // "BUSINESS" | "PERSONAL"
  frequency         String  @default("DAILY") // "DAILY" | "WEEKLY" | "CUSTOM"
  targetDaysPerWeek Int?
  daysOfWeek        String? // "MON,WED,FRI" — used when frequency = CUSTOM
  color             String?
  active            Boolean @default(true)
  order             Int     @default(0)

  logs HabitLog[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([area, active])
}

model HabitLog {
  id        String   @id @default(cuid())
  habitId   String
  habit     Habit    @relation(fields: [habitId], references: [id], onDelete: Cascade)
  date      DateTime // normalized to UTC midnight, see src/lib/dates.ts
  completed Boolean  @default(true)
  note      String?
  createdAt DateTime @default(now())

  @@unique([habitId, date])
  @@index([date])
}

model Document {
  id             String    @id @default(cuid())
  name           String // e.g. "Driver's License", "Passport"
  category       String    @default("OTHER") // "LICENSE" | "PASSPORT" | "INSURANCE" | "REGISTRATION" | "VISA" | "OTHER"
  area           String // "BUSINESS" | "PERSONAL"
  documentNumber String?
  issueDate      DateTime?
  expiryDate     DateTime?
  notes          String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([area])
  @@index([expiryDate])
}

model NotificationRecipient {
  id           String   @id @default(cuid())
  label        String?
  email        String?
  whatsapp     String? // E.164, e.g. +15551234567
  dailyDigest  Boolean  @default(true)
  weeklyDigest Boolean  @default(true)
  active       Boolean  @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
